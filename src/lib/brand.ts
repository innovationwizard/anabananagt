// ---------------------------------------------------------------------------
// Colores de marca para superficies que no pueden usar los tokens CSS
// (correos HTML, imágenes OG). Fuente: manual de marca / globals.css @theme.
// ---------------------------------------------------------------------------

export const BRAND = {
  /** Azul marino profundo — color primario. */
  navy: "#243054",
  /** Azul eléctrico — acento. */
  blue: "#2E46D4",
  /** Gris de texto en correos. */
  emailText: "#374151",
  /** Gris tenue. */
  emailMuted: "#6B7280",
  /** Bordes claros. */
  emailBorder: "#E2E2E2",
  /** Fondo suave. */
  emailSurface: "#F6F7F9",
} as const;
